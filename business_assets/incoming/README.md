# Incoming Assets

Drop raw files here before processing into project folders.

## Workflow

1. Save WhatsApp/email downloads to `incoming/`
2. Rename using convention (never keep WhatsApp names)
3. Move to correct `{status}_projects/{slug}/originals/`
4. Create metadata JSON in `metadata/assets/`
5. Update `project.json` and project `README.md`

## Backup

- `_backup-manifest-2026-06-28.json` — SHA256 of all 17 legacy files (Phase 0)

## Last Updated

2026-06-28
